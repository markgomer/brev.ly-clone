{
  "name": "brev.ly",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "backend": "pushd backend && bun dev && popd",
    "frontend": "pushd frontend && bun dev && popd",
    "start": "NODE_ENV=production bun frontend/src/index.ts && bun backend/src/server.ts"
  },
}
