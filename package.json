{
  "name": "brev.ly",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"bun --cwd backend dev\" \"bun --cwd frontend dev\"",
    "backend": "bun --cwd backend dev",
    "frontend": "bun --cwd frontend dev",
    "start": "NODE_ENV=production bun --cwd backend src/server.ts & bun --cwd frontend src/index.ts & wait"
  },
  "devDependencies": {
    "concurrently": "9.2.1"
  }
}
