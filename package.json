{
  "name": "brev.ly",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"bun --cwd server dev\" \"bun --cwd web dev\"",
    "server": "bun --cwd server dev",
    "web": "bun --cwd web dev",
    "start": "NODE_ENV=production bun --cwd server src/server.ts & bun --cwd web src/index.ts & wait"
  },
  "devDependencies": {
    "concurrently": "9.2.1"
  }
}
